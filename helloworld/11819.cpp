#include <iostream>

using namespace std;

int main(void){
    ios::sync_with_stdio(0);
    cin.tie(0);

    long long a, b, c;
    cin>>a>>b>>c;
    int check = 1;
    long long answer= 1;
    for(int i = 0; i<b/2; i++){
        if(check){
            if
            answer *= a/c;
        }
        answer *= a;
    }
    cout<< answer;

}